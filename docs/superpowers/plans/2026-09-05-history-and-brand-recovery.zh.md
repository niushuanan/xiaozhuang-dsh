# 历史与品牌恢复实施计划

[English](2026-09-05-history-and-brand-recovery.md) | 中文

> **供执行 agent 使用：** 每项独立修复都采用系统调试与测试驱动开发；协调者集成并验证完整用户路径。

**目标：** 让已有导入聊天和 Agent 对话可查找、可读取，并恢复 Xiaozhuang DSH 品牌，不改原始会话代际文件。

**架构：** 会话日志保持权威性；身份校验后的上一代缓存只作为列表提示。产品品牌属于既有插件管理器中可撤销的侧栏 slot 贡献。

**技术栈：** TypeScript、Cordis、React、Vitest、Playwright CLI、仅追加 JSONL 代际文件。

**需求：** 用户当前要求恢复导入与 Agent 历史、恢复左上角品牌，最终统一提交一次并推送。

## 全局约束

- 不做哈希对比，不重写原始日志，不重新导入，不批量取消归档。
- 保留全部 18 个产品插件目录及既有无关改动。
- 未获用户明确允许，不重启运行中的 3080 实例。
- 执行定向回归与真实浏览器验收；不重新安装工作区，不跑无关全量套件。
- 整体修复只提交一次；主仓推送后才发布受影响插件副本。

## 任务 1：恢复历史最近活动时间

**文件：** `packages/api/session-controller/src/list.ts`、`packages/api/session-controller/tests/session-cold.host.spec.ts`、所属双语 README 和 Agent Note。

- [x] 扩展冷列表回归：上一代聊天创建于 100，最近提问于 1200，应排在创建于 200 的无缓存记录之前，且不读取 stat 或正文。
- [x] 通过纯展示导航缓存在 `title`、`agentPreset` 之外请求 `sessionListMetadata`；位置保持未知，严格回放身份单独处理。
- [x] 执行冷控制器与投影缓存测试；将真实列表时间和保留的历史元数据进行核对。

## 任务 2：核对完整历史并恢复访问

**文件：** 仅在精确旧事件语义需要修复时修改相邻会话格式包；仅为已复现导航回归修改 `packages/client/ui-workspace/`。

- [x] 对账 157 个 DeepSeek 源会话、已导入持久标识与后台列表。
- [x] 不改原件，比较旧文件与 v2 消息内容；检查此前被拒绝的两份旧 Agent 会话。
- [x] 必要迁移改动先用源自真实数据的有界 fixture 证明错误，再仅实现已核实语义。
- [x] 获准重启 Host 后，从真实侧栏／搜索打开导入和旧 Agent 对话，检查历史并刷新。

## 任务 3：恢复产品品牌

**文件：** `plugins/plugin-manager/` 客户端组件、可撤销注册、定向组装测试及双语 README。

- [x] 证明已安装产品插件没有向 `sidebar.brand.name` 提供品牌。
- [x] 通过该 slot 提供既有 Xiaozhuang DSH 名称；插件缺席时保留上游回退。
- [x] 验证注册、渲染品牌、释放和真实浏览器页头。
- [x] 通过功能自有的可选 slot 恢复 9 个原有设置图标；验证每个真实导航入口和插件释放，不在核心添加本地插件 id。

## 任务 4：集成与交付

- [x] 审查整体差异，执行定向检查，检查真实截图与浏览器错误。
- [x] 用已验证结果和明确剩余限制更新根 README 配对及 PROJECT_CONTEXT。
- [ ] 只暂存本次改动，统一提交一次并推送 `origin/master`；不用摘要对比验证远端内容。
- [ ] 只发布受影响独立插件副本，清理自有临时目录。
