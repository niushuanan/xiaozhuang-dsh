# Computer Use 包

[English](README.md) | 中文

`computer-use` 负责原生用户交互运行时。`@deepseek-ai/dsh-computer-use` 提供 `/computer` 与 `/browser`，只向接收命令的 Agent 注册对应工具，管理干净的 Playwright 上下文，并通过当前 Web 服务对已解压的 DSH Browser Bridge 扩展进行本地鉴权。
