# Computer Use packages

English | [中文](README.zh.md)

`computer-use` owns the native user-interaction runtime. `@deepseek-ai/dsh-computer-use` exposes `/computer` and `/browser`, scopes their tools to the receiving Agent, manages clean Playwright contexts, and authenticates the unpacked DSH Browser Bridge extension over the active Web server.
